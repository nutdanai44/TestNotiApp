# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestNoti' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'Firebase/Core'
pod 'Firebase/Messaging'
pod 'Firebase/Auth'

pod 'Firebase/InAppMessagingDisplay'

pod 'Firebase/RemoteConfig'

pod 'RealmSwift'
pod 'ObjectMapper', '~> 3.2'

pod 'RxSwift',    '~> 4.0'
pod 'RxCocoa',    '~> 4.0'

pod 'SDWebImage', '~> 4.0'
pod 'whoewx3yCommon'

pod 'ApiAI', '~> 0.7'

  # Pods for TestNoti

  target 'TestNotiTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestNotiUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
